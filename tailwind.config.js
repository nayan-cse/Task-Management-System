/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        './src/**/*.{js,ts,jsx,tsx}', // Tailwind will scan all JS, TS, JSX, and TSX files in your src directory
    ],
    theme: {
        extend: {},
    },
    plugins: [],
};
